const { expires_at } = require('./google-credentials.json');
// const validQuery = ['country', 'likes', 'privacy', 'desc']

// const reqData = {
//     country: 'wkwk',
//     privacy: 'wkwkw',
//     wkkw: 'kkk'
// }

// const reqDataArray = Object.keys(reqData);
// const validObject = validQuery.map((value) => reqDataArray.map((req) => value.toString() === req.toString() && value).filter((req) => req !== false)[0]).filter((value) => value !== undefined).length === reqDataArray.length

// console.log(validObject);


// (function () {
//     const hours = new Date(expires_at).getHours();
//     const minutes = new Date(expires_at).getMinutes();

//     console.log({ hours, minutes });
// })()